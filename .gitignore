# --- Clojure & Java ---
# The local port file created when you start the REPL
.nrepl-port
.shadow-cljs/

# Cache files generated by the Clojure CLI
.cpcache/

# Build artifacts (where compiled code/jars go)
target/
classes/

# Local configuration overrides (optional, but good practice)
deps.local.edn
profiles.clj

# --- Emacs / Spacemacs ---
# Backup files (tilde at the end)
*~

# Auto-save files (hashes)
\#*\#

# Lock files (symlinks created while editing)
.#*

# Flycheck / LSP cache files
.lsp/
flycheck_*

# --- OS / Misc ---
.DS_Store
*.log

# Ignore the internal cache, but keep the config
.clj-kondo/.cache/
